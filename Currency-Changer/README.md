# built a currency converter app using tailwindcss for styling

1. use a custom hook called 'useCurrencyInfo' to use it for fetching the data from json currency info api and parsing it to populate currency options (array) and their equivalent values for conversion.
2. created a component called InputBox.jsx for creating the both input and output container
3. other feature includes a swapping button which swap the input and output currency type
